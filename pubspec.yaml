name: crud_backend_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  bcrypt: ^1.1.3
  dart_amqp: ^0.3.1
  dart_jsonwebtoken: ^2.17.0
  flutter:
    sdk: flutter
  mongo_dart: ^0.10.4
  redis: ^4.0.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.8
  mockito: ^5.4.5
  build_runner: ^2.4.14

flutter:
  uses-material-design: true
